import React from 'react';

const TextInput = React.forwardRef(( {
    type, placeholder, styles, label, labelStylese, register, name, error
}) => {
    return <div>TextInput</div>
});

export default TextInput